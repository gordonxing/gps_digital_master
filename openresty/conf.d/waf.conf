# WAF 配置（在 server 级别加载）
# 实际 WAF 逻辑通过 access_by_lua_file 在 lucky.conf 中引用
# 此文件用于 WAF 相关的额外 server 级别配置

# WAF 日志
log_format waf_log '$remote_addr [$time_local] "$request" '
                   '$status "blocked_by:$waf_block_reason"';
